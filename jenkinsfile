
node
{
def app

    
        stage('Build') {
            app = docker.build("ashurana31/firstimage")
        }
        stage('Pushing') {
            
                docker.withRegistry('','Dockerhub_credentials')
                {
                  app.push('$(env.BUILD_NUMBER)')
                  app.push("latest")
                }
            
        }
        
    

}
