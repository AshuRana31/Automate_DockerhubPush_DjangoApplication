pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh label: '', script: '''#! /bin/bash
                sudo docker build .'''
            }
        }
        
    }
}
