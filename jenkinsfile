pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                docker.build('AshuRana31/Automate_DockerhubPush_DjangoApplication')
            }
        }
    }
}
